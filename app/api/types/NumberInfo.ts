export interface NumberInfo {
    id: number
    name: string
    where_did: string
    setuprate: number
    fixrate: number
    incoming_per_minute: number
    tollfree_rate_in_min: number
    incoming_rate_sms: number
    docs: string
}