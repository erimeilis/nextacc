export type NumberInfo = {
    did: string
    name: string
    where_did: string
    setuprate: string
    fixrate: string
    voice: boolean
    sms: boolean
    tollfree: boolean
    fax: boolean
    incoming_per_minute: string
    tollfree_rate_in_min: string
    incoming_rate_sms: string
    docs: [] | string
}